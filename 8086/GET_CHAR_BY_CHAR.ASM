.MODEL SMALL
.STACK 100H
.DATA
	ARRAY DB ?
.CODE
BEGIN:
	MOV AX, @DATA
	MOV DS, AX
	

GET:	LEA SI, ARRAY
NONL:	MOV AH, 01H
	INT 21H
	MOV [SI], AL
	INC SI
	CMP AL, 0DH
	JNE NONL
	MOV [SI], '$'
	LEA DX, ARRAY
	MOV AH, 09H
	INT 21H

	MOV AH, 4CH
	INT 21H
END BEGIN
